import React from 'react';
import ReactComponent from '../components/ReactComponent';

const ReactPage = () => {
    return (
        <div>
            <h1>Página React</h1>
            <ReactComponent />
        </div>
    );
};

export default ReactPage;
